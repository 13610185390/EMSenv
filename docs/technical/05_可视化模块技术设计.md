# 可视化模块技术设计文档

> 模块名称：visualization
> 版本：v0.2
> 日期：2026-01-08

---

## 1. 模块概述

### 1.1 功能描述
可视化模块负责将仿真结果以图表形式展示，包括功率曲线、SOC曲线、经济分析图表等，同时提供数据导出功能。**本模块集成教学辅助功能，通过图表标注、交互提示帮助学员理解数据含义和运行规律。**

### 1.2 模块职责
- 绘制时序曲线图（功率、SOC、电价等）
- 绘制统计分析图表
- 绘制经济分析图表
- 提供交互式图表功能
- 数据导出（CSV、Excel）
- 报告生成（预留）
- **提供图表教学标注**
- **展示关键数据点说明**
- **支持图表交互式学习**

### 1.3 依赖关系
```
visualization.py
    ├── models/simulation_result.py  # 仿真结果数据
    ├── matplotlib                   # 静态图表
    ├── plotly                       # 交互式图表
    ├── pandas                       # 数据处理
    └── modules/learning_center.py   # 教学知识关联
```

---

## 2. 数据结构定义

### 2.1 图表配置数据类

```python
from dataclasses import dataclass, field
from typing import Optional, List, Dict, Any, Tuple
from enum import Enum


# === 可视化教学元数据 ===
VISUALIZATION_TEACHING_META: Dict[str, Dict[str, Any]] = {
    # 图表类型说明
    "power_curve": {
        "title": "功率曲线图",
        "help_text": "展示负荷、储能充放电、电网功率随时间的变化",
        "key_observations": [
            "负荷曲线（红色）：用户的实际用电需求",
            "充电功率（青色向下）：储能从电网吸收电能",
            "放电功率（蓝色向上）：储能向负荷供电",
            "电网功率（黄色虚线）：实际从电网购电的功率"
        ],
        "learning_focus": "观察储能如何在峰谷时段进行充放电，理解削峰填谷原理",
        "learning_points": ["2.2", "3.1"]
    },
    "soc_curve": {
        "title": "SOC变化曲线",
        "help_text": "展示电池荷电状态(SOC)随时间的变化",
        "key_observations": [
            "SOC曲线（绿色）：电池剩余电量百分比",
            "运行范围（绿色区域）：正常运行的SOC区间",
            "保护边界（红色虚线）：触发保护的SOC阈值"
        ],
        "learning_focus": "理解SOC变化与充放电的关系，观察保护逻辑的触发时机",
        "learning_points": ["1.2", "4.2"]
    },
    "price_curve": {
        "title": "电价曲线",
        "help_text": "展示24小时分时电价变化",
        "key_observations": [
            "谷时段（低价）：适合充电储能",
            "峰时段（高价）：适合放电获利",
            "平时段：根据SOC状态决定是否运行"
        ],
        "learning_focus": "理解峰谷电价套利的基本原理",
        "learning_points": ["2.3", "3.1"]
    },
    "energy_summary": {
        "title": "能量统计图",
        "help_text": "柱状图展示充电量、放电量及损耗",
        "key_observations": [
            "充电量 > 放电量：因为存在效率损失",
            "损耗 = 充电量 - 放电量",
            "往返效率 = 放电量/充电量 × 100%"
        ],
        "learning_focus": "理解储能系统的能量转换效率",
        "learning_points": ["1.3"]
    },
    "economic_summary": {
        "title": "经济分析图",
        "help_text": "展示收益构成和投资回收分析",
        "key_observations": [
            "套利利润 = 放电收益 - 充电成本",
            "年化收益基于日收益×365天估算",
            "回收期 = 初始投资/净年收益"
        ],
        "learning_focus": "理解储能项目经济性评估方法",
        "learning_points": ["2.3"]
    },

    # 图表交互功能
    "hover_tooltip": {
        "help_text": "鼠标悬停显示数据详情",
        "usage": "将鼠标移至曲线上任意点，查看该时刻的精确数值"
    },
    "zoom_pan": {
        "help_text": "缩放和平移查看细节",
        "usage": "使用鼠标滚轮缩放，拖拽平移，双击重置视图"
    },
    "legend_toggle": {
        "help_text": "图例开关控制显示内容",
        "usage": "点击图例项可显示/隐藏对应曲线，便于对比分析"
    },

    # 数据导出说明
    "export_csv": {
        "help_text": "导出为CSV格式，可用Excel打开",
        "content": "包含所有时间步的仿真数据",
        "learning_points": ["4.1"]
    },
    "export_excel": {
        "help_text": "导出为Excel格式，包含多个工作表",
        "content": "时序数据表 + 统计汇总表",
        "learning_points": ["4.1"]
    }
}


class ChartType(Enum):
    """图表类型"""
    LINE = "折线图"
    AREA = "面积图"
    BAR = "柱状图"
    STEP = "阶梯图"
    SCATTER = "散点图"
    # 🔲 扩展预留
    EXT_CHART_1 = "_扩展图表1"


class ChartLibrary(Enum):
    """图表库选择"""
    MATPLOTLIB = "matplotlib"
    PLOTLY = "plotly"


@dataclass
class ChartStyle:
    """图表样式配置"""

    # 颜色方案
    colors: Dict[str, str] = field(default_factory=lambda: {
        'load': '#FF6B6B',          # 负荷 - 红色
        'bess_charge': '#4ECDC4',   # 充电 - 青色
        'bess_discharge': '#45B7D1', # 放电 - 蓝色
        'soc': '#96CEB4',           # SOC - 绿色
        'grid': '#FFEAA7',          # 电网 - 黄色
        'price': '#DDA0DD',         # 电价 - 紫色
        'peak': '#FF6B6B',          # 峰时 - 红色
        'flat': '#FFE66D',          # 平时 - 黄色
        'valley': '#4ECDC4',        # 谷时 - 青色
    })

    # 图表尺寸
    figure_size: Tuple[int, int] = (12, 6)
    subplot_height: int = 4

    # 字体设置
    font_family: str = "SimHei"     # 支持中文
    title_size: int = 14
    label_size: int = 12
    tick_size: int = 10
    legend_size: int = 10

    # 网格
    show_grid: bool = True
    grid_alpha: float = 0.3

    # 🔲 扩展预留
    ext_style: Dict[str, Any] = field(default_factory=dict)


@dataclass
class ChartConfig:
    """单个图表配置"""

    title: str
    chart_type: ChartType = ChartType.LINE
    x_label: str = ""
    y_label: str = ""
    y_unit: str = ""
    show_legend: bool = True
    legend_loc: str = "upper right"

    # 数据系列配置
    series: List[Dict[str, Any]] = field(default_factory=list)
    # series格式: [{'name': '负荷', 'data': [...], 'color': '#FF6B6B'}, ...]

    # 双Y轴配置
    secondary_y: bool = False
    secondary_y_label: str = ""
    secondary_y_unit: str = ""

    # 区域填充
    fill_between: Optional[Dict[str, Any]] = None

    # 水平参考线
    hlines: List[Dict[str, Any]] = field(default_factory=list)
    # hlines格式: [{'y': 90, 'label': 'SOC上限', 'color': 'red', 'linestyle': '--'}]

    # 🔲 扩展预留
    ext_config: Dict[str, Any] = field(default_factory=dict)
```

### 2.2 导出配置数据类

```python
@dataclass
class ExportConfig:
    """导出配置"""

    # 文件格式
    format: str = "csv"             # csv, xlsx, json

    # 包含的数据
    include_timestep_data: bool = True
    include_summary: bool = True
    include_charts: bool = False    # 是否导出图表图片

    # 时间格式
    time_format: str = "hours"      # hours, datetime

    # 小数位数
    decimal_places: int = 2

    # 🔲 扩展预留
    ext_export: Dict[str, Any] = field(default_factory=dict)
```

---

## 3. 接口设计

### 3.1 模块公开接口

```python
# visualization.py 公开接口

def plot_power_curve(
    result,                         # SimulationResult
    style: Optional[ChartStyle] = None,
    library: ChartLibrary = ChartLibrary.PLOTLY
) -> Any:
    """
    绘制功率曲线图

    包含：负荷曲线、储能功率、净负荷、电网功率

    Returns:
        matplotlib.Figure 或 plotly.Figure
    """
    pass


def plot_soc_curve(
    result,                         # SimulationResult
    style: Optional[ChartStyle] = None,
    library: ChartLibrary = ChartLibrary.PLOTLY
) -> Any:
    """
    绘制SOC变化曲线

    包含：SOC曲线、SOC上下限参考线

    Returns:
        matplotlib.Figure 或 plotly.Figure
    """
    pass


def plot_price_curve(
    price_params,                   # ElectricityPriceParams
    style: Optional[ChartStyle] = None,
    library: ChartLibrary = ChartLibrary.MATPLOTLIB
) -> Any:
    """
    绘制电价曲线

    Returns:
        matplotlib.Figure 或 plotly.Figure
    """
    pass


def plot_energy_summary(
    result,                         # SimulationResult
    style: Optional[ChartStyle] = None,
    library: ChartLibrary = ChartLibrary.PLOTLY
) -> Any:
    """
    绘制能量统计柱状图

    包含：充电量、放电量、损耗

    Returns:
        matplotlib.Figure 或 plotly.Figure
    """
    pass


def plot_economic_summary(
    result,                         # SimulationResult
    annual_projection: Dict[str, float],
    style: Optional[ChartStyle] = None,
    library: ChartLibrary = ChartLibrary.PLOTLY
) -> Any:
    """
    绘制经济分析图表

    Returns:
        matplotlib.Figure 或 plotly.Figure
    """
    pass


def plot_combined_dashboard(
    result,                         # SimulationResult
    style: Optional[ChartStyle] = None
) -> Any:
    """
    绘制综合仪表板（多图组合）

    Returns:
        plotly.Figure
    """
    pass


def export_results(
    result,                         # SimulationResult
    filepath: str,
    config: Optional[ExportConfig] = None
) -> bool:
    """
    导出仿真结果

    Args:
        result: 仿真结果
        filepath: 导出文件路径
        config: 导出配置

    Returns:
        bool: 导出是否成功
    """
    pass


def generate_statistics_table(
    result,                         # SimulationResult
    table_type: str = "economic"    # economic, operation, annual
) -> List[List[str]]:
    """
    生成统计表格数据

    Returns:
        List[List[str]]: [[指标, 数值, 单位], ...]
    """
    pass


# 🔲 扩展接口预留
def register_chart_type(
    name: str,
    plot_func: callable
) -> bool:
    """注册自定义图表类型"""
    pass


def generate_report(
    result,                         # SimulationResult
    template: str = "default"
) -> str:
    """生成PDF报告（预留）"""
    pass
```

---

## 4. 核心逻辑

### 4.1 功率曲线绘制

```python
import matplotlib.pyplot as plt
import plotly.graph_objects as go
from plotly.subplots import make_subplots
import numpy as np


def plot_power_curve(
    result,
    style: Optional[ChartStyle] = None,
    library: ChartLibrary = ChartLibrary.PLOTLY
):
    """绘制功率曲线"""

    if style is None:
        style = ChartStyle()

    time = result.get_time_array()
    load = result.get_load_array()
    bess_power = result.get_power_array()
    grid_power = result.get_grid_power_array()
    price = result.get_price_array()

    # 分离充放电功率
    charge_power = np.where(bess_power < 0, -bess_power, 0)
    discharge_power = np.where(bess_power > 0, bess_power, 0)

    if library == ChartLibrary.PLOTLY:
        return _plot_power_curve_plotly(
            time, load, charge_power, discharge_power, grid_power, price, style
        )
    else:
        return _plot_power_curve_matplotlib(
            time, load, charge_power, discharge_power, grid_power, price, style
        )


def _plot_power_curve_plotly(
    time, load, charge_power, discharge_power, grid_power, price, style
):
    """使用Plotly绘制功率曲线"""

    fig = make_subplots(
        rows=2, cols=1,
        shared_xaxes=True,
        vertical_spacing=0.1,
        subplot_titles=('功率曲线', '电价曲线'),
        row_heights=[0.7, 0.3]
    )

    # 负荷曲线
    fig.add_trace(
        go.Scatter(
            x=time, y=load,
            name='负荷',
            line=dict(color=style.colors['load'], width=2),
            fill='tozeroy',
            fillcolor=f"rgba(255, 107, 107, 0.1)"
        ),
        row=1, col=1
    )

    # 充电功率（显示为负值）
    fig.add_trace(
        go.Scatter(
            x=time, y=-charge_power,
            name='充电功率',
            line=dict(color=style.colors['bess_charge'], width=2),
            fill='tozeroy',
            fillcolor=f"rgba(78, 205, 196, 0.3)"
        ),
        row=1, col=1
    )

    # 放电功率
    fig.add_trace(
        go.Scatter(
            x=time, y=discharge_power,
            name='放电功率',
            line=dict(color=style.colors['bess_discharge'], width=2),
            fill='tozeroy',
            fillcolor=f"rgba(69, 183, 209, 0.3)"
        ),
        row=1, col=1
    )

    # 电网功率
    fig.add_trace(
        go.Scatter(
            x=time, y=grid_power,
            name='电网功率',
            line=dict(color=style.colors['grid'], width=1.5, dash='dash')
        ),
        row=1, col=1
    )

    # 电价曲线
    fig.add_trace(
        go.Scatter(
            x=time, y=price,
            name='电价',
            line=dict(color=style.colors['price'], width=2),
            fill='tozeroy',
            fillcolor=f"rgba(221, 160, 221, 0.2)"
        ),
        row=2, col=1
    )

    # 布局设置
    fig.update_layout(
        height=500,
        showlegend=True,
        legend=dict(
            orientation="h",
            yanchor="bottom",
            y=1.02,
            xanchor="right",
            x=1
        ),
        hovermode='x unified'
    )

    fig.update_xaxes(title_text="时间 (h)", row=2, col=1)
    fig.update_yaxes(title_text="功率 (kW)", row=1, col=1)
    fig.update_yaxes(title_text="电价 (元/kWh)", row=2, col=1)

    return fig


def _plot_power_curve_matplotlib(
    time, load, charge_power, discharge_power, grid_power, price, style
):
    """使用Matplotlib绘制功率曲线"""

    plt.rcParams['font.sans-serif'] = [style.font_family]
    plt.rcParams['axes.unicode_minus'] = False

    fig, (ax1, ax2) = plt.subplots(
        2, 1,
        figsize=style.figure_size,
        sharex=True,
        gridspec_kw={'height_ratios': [3, 1]}
    )

    # 功率曲线
    ax1.plot(time, load, label='负荷', color=style.colors['load'], linewidth=2)
    ax1.fill_between(time, 0, -charge_power, label='充电',
                     color=style.colors['bess_charge'], alpha=0.5)
    ax1.fill_between(time, 0, discharge_power, label='放电',
                     color=style.colors['bess_discharge'], alpha=0.5)
    ax1.plot(time, grid_power, label='电网', color=style.colors['grid'],
             linewidth=1.5, linestyle='--')

    ax1.axhline(y=0, color='gray', linewidth=0.5)
    ax1.set_ylabel('功率 (kW)', fontsize=style.label_size)
    ax1.legend(loc='upper right', fontsize=style.legend_size)
    ax1.grid(style.show_grid, alpha=style.grid_alpha)
    ax1.set_title('功率曲线', fontsize=style.title_size)

    # 电价曲线
    ax2.step(time, price, where='post', label='电价',
             color=style.colors['price'], linewidth=2)
    ax2.fill_between(time, price, step='post',
                     color=style.colors['price'], alpha=0.2)
    ax2.set_xlabel('时间 (h)', fontsize=style.label_size)
    ax2.set_ylabel('电价 (元/kWh)', fontsize=style.label_size)
    ax2.grid(style.show_grid, alpha=style.grid_alpha)

    plt.tight_layout()
    return fig
```

### 4.2 SOC曲线绘制

```python
def plot_soc_curve(
    result,
    style: Optional[ChartStyle] = None,
    library: ChartLibrary = ChartLibrary.PLOTLY
):
    """绘制SOC曲线"""

    if style is None:
        style = ChartStyle()

    time = result.get_time_array()
    soc = result.get_soc_array()

    # 获取SOC限制
    soc_min = result.bess_params.soc_min
    soc_max = result.bess_params.soc_max
    protect_low = result.control_params.protection.protect_soc_low
    protect_high = result.control_params.protection.protect_soc_high

    if library == ChartLibrary.PLOTLY:
        return _plot_soc_curve_plotly(
            time, soc, soc_min, soc_max, protect_low, protect_high, style
        )
    else:
        return _plot_soc_curve_matplotlib(
            time, soc, soc_min, soc_max, protect_low, protect_high, style
        )


def _plot_soc_curve_plotly(
    time, soc, soc_min, soc_max, protect_low, protect_high, style
):
    """使用Plotly绘制SOC曲线"""

    fig = go.Figure()

    # SOC曲线
    fig.add_trace(
        go.Scatter(
            x=time, y=soc,
            name='SOC',
            line=dict(color=style.colors['soc'], width=2.5),
            fill='tozeroy',
            fillcolor=f"rgba(150, 206, 180, 0.3)"
        )
    )

    # SOC运行范围区域
    fig.add_hrect(
        y0=soc_min, y1=soc_max,
        fillcolor="rgba(150, 206, 180, 0.1)",
        line_width=0,
        annotation_text="运行范围",
        annotation_position="right"
    )

    # 参考线
    fig.add_hline(y=soc_max, line_dash="dash",
                  line_color="green", annotation_text=f"SOC上限 {soc_max}%")
    fig.add_hline(y=soc_min, line_dash="dash",
                  line_color="green", annotation_text=f"SOC下限 {soc_min}%")
    fig.add_hline(y=protect_high, line_dash="dot",
                  line_color="red", annotation_text=f"过充保护 {protect_high}%")
    fig.add_hline(y=protect_low, line_dash="dot",
                  line_color="red", annotation_text=f"过放保护 {protect_low}%")

    fig.update_layout(
        title='SOC变化曲线',
        xaxis_title='时间 (h)',
        yaxis_title='SOC (%)',
        yaxis_range=[0, 100],
        height=400,
        showlegend=True,
        hovermode='x'
    )

    return fig


def _plot_soc_curve_matplotlib(
    time, soc, soc_min, soc_max, protect_low, protect_high, style
):
    """使用Matplotlib绘制SOC曲线"""

    plt.rcParams['font.sans-serif'] = [style.font_family]
    plt.rcParams['axes.unicode_minus'] = False

    fig, ax = plt.subplots(figsize=(style.figure_size[0], style.subplot_height))

    # SOC曲线
    ax.plot(time, soc, color=style.colors['soc'], linewidth=2.5, label='SOC')
    ax.fill_between(time, soc, alpha=0.3, color=style.colors['soc'])

    # 运行范围区域
    ax.axhspan(soc_min, soc_max, alpha=0.1, color='green', label='运行范围')

    # 参考线
    ax.axhline(y=soc_max, color='green', linestyle='--',
               linewidth=1, label=f'SOC上限 {soc_max}%')
    ax.axhline(y=soc_min, color='green', linestyle='--',
               linewidth=1, label=f'SOC下限 {soc_min}%')
    ax.axhline(y=protect_high, color='red', linestyle=':',
               linewidth=1, label=f'过充保护 {protect_high}%')
    ax.axhline(y=protect_low, color='red', linestyle=':',
               linewidth=1, label=f'过放保护 {protect_low}%')

    ax.set_xlabel('时间 (h)', fontsize=style.label_size)
    ax.set_ylabel('SOC (%)', fontsize=style.label_size)
    ax.set_ylim(0, 100)
    ax.set_title('SOC变化曲线', fontsize=style.title_size)
    ax.legend(loc='upper right', fontsize=style.legend_size)
    ax.grid(style.show_grid, alpha=style.grid_alpha)

    plt.tight_layout()
    return fig
```

### 4.3 统计表格生成

```python
def generate_statistics_table(
    result,
    table_type: str = "economic"
) -> List[List[str]]:
    """生成统计表格数据"""

    if table_type == "economic":
        return _generate_economic_table(result)
    elif table_type == "operation":
        return _generate_operation_table(result)
    elif table_type == "annual":
        return _generate_annual_table(result)
    else:
        return []


def _generate_economic_table(result) -> List[List[str]]:
    """生成经济统计表格"""
    return [
        ["充电成本", f"{result.total_charge_cost:.2f}", "元"],
        ["放电收益", f"{result.total_discharge_revenue:.2f}", "元"],
        ["套利利润", f"{result.arbitrage_profit:.2f}", "元"],
        ["基准电费", f"{result.baseline_cost:.2f}", "元"],
        ["节约电费", f"{result.cost_saving:.2f}", "元"],
    ]


def _generate_operation_table(result) -> List[List[str]]:
    """生成运行统计表格"""
    return [
        ["总充电量", f"{result.total_charge_kwh:.2f}", "kWh"],
        ["总放电量", f"{result.total_discharge_kwh:.2f}", "kWh"],
        ["系统效率", f"{result.system_efficiency:.1f}", "%"],
        ["等效循环", f"{result.equivalent_cycles:.2f}", "次"],
        ["容量利用率", f"{result.capacity_utilization:.1f}", "%"],
        ["功率利用率", f"{result.power_utilization:.1f}", "%"],
        ["最低SOC", f"{result.soc_min_actual:.1f}", "%"],
        ["最高SOC", f"{result.soc_max_actual:.1f}", "%"],
        ["最终SOC", f"{result.soc_final:.1f}", "%"],
    ]


def _generate_annual_table(result, annual_data: Dict) -> List[List[str]]:
    """生成年度预测表格"""
    return [
        ["日均收益", f"{annual_data.get('daily_profit', 0):.2f}", "元/天"],
        ["年化收益", f"{annual_data.get('annual_revenue', 0):.0f}", "元/年"],
        ["年运营成本", f"{annual_data.get('annual_opex', 0):.0f}", "元/年"],
        ["净年收益", f"{annual_data.get('net_annual_revenue', 0):.0f}", "元/年"],
        ["初始投资", f"{annual_data.get('initial_investment', 0):.0f}", "元"],
        ["静态回收期", f"{annual_data.get('static_payback', 0):.1f}", "年"],
        ["年循环次数", f"{annual_data.get('annual_cycles', 0):.0f}", "次"],
        ["预计寿命", f"{annual_data.get('expected_life_years', 0):.1f}", "年"],
    ]
```

### 4.4 数据导出

```python
import pandas as pd
from datetime import datetime, timedelta


def export_results(
    result,
    filepath: str,
    config: Optional[ExportConfig] = None
) -> bool:
    """导出仿真结果"""

    if config is None:
        config = ExportConfig()

    try:
        if config.format == "csv":
            return _export_csv(result, filepath, config)
        elif config.format == "xlsx":
            return _export_excel(result, filepath, config)
        elif config.format == "json":
            return _export_json(result, filepath, config)
        else:
            raise ValueError(f"不支持的导出格式: {config.format}")
    except Exception as e:
        print(f"导出失败: {e}")
        return False


def _export_csv(result, filepath: str, config: ExportConfig) -> bool:
    """导出为CSV"""

    df = result.to_dataframe()

    # 格式化时间
    if config.time_format == "datetime":
        base_time = datetime.now().replace(hour=0, minute=0, second=0, microsecond=0)
        df['datetime'] = df['time_hours'].apply(
            lambda h: base_time + timedelta(hours=h)
        )

    # 四舍五入
    numeric_cols = df.select_dtypes(include=['float64']).columns
    df[numeric_cols] = df[numeric_cols].round(config.decimal_places)

    df.to_csv(filepath, index=False, encoding='utf-8-sig')
    return True


def _export_excel(result, filepath: str, config: ExportConfig) -> bool:
    """导出为Excel"""

    with pd.ExcelWriter(filepath, engine='openpyxl') as writer:
        # 时序数据
        if config.include_timestep_data:
            df = result.to_dataframe()
            df.to_excel(writer, sheet_name='时序数据', index=False)

        # 汇总统计
        if config.include_summary:
            summary_df = pd.DataFrame({
                '类别': ['运行统计'] * 9 + ['经济统计'] * 5,
                '指标': [
                    '总充电量', '总放电量', '系统效率', '等效循环',
                    '容量利用率', '功率利用率', '最低SOC', '最高SOC', '最终SOC',
                    '充电成本', '放电收益', '套利利润', '基准电费', '节约电费'
                ],
                '数值': [
                    result.total_charge_kwh, result.total_discharge_kwh,
                    result.system_efficiency, result.equivalent_cycles,
                    result.capacity_utilization, result.power_utilization,
                    result.soc_min_actual, result.soc_max_actual, result.soc_final,
                    result.total_charge_cost, result.total_discharge_revenue,
                    result.arbitrage_profit, result.baseline_cost, result.cost_saving
                ],
                '单位': [
                    'kWh', 'kWh', '%', '次', '%', '%', '%', '%', '%',
                    '元', '元', '元', '元', '元'
                ]
            })
            summary_df.to_excel(writer, sheet_name='统计汇总', index=False)

    return True


def _export_json(result, filepath: str, config: ExportConfig) -> bool:
    """导出为JSON"""

    import json

    data = {
        'config': {
            'duration_hours': result.config.duration_hours,
            'timestep_minutes': result.config.timestep_minutes,
        },
        'summary': {
            'total_charge_kwh': result.total_charge_kwh,
            'total_discharge_kwh': result.total_discharge_kwh,
            'system_efficiency': result.system_efficiency,
            'equivalent_cycles': result.equivalent_cycles,
            'arbitrage_profit': result.arbitrage_profit,
        }
    }

    if config.include_timestep_data:
        data['timestep_data'] = result.to_dataframe().to_dict(orient='records')

    with open(filepath, 'w', encoding='utf-8') as f:
        json.dump(data, f, ensure_ascii=False, indent=2)

    return True
```

---

## 5. UI集成

### 5.1 图表显示函数

```python
def update_power_plot(result) -> go.Figure:
    """更新功率曲线图（Gradio回调）"""
    if result is None:
        return go.Figure()
    return plot_power_curve(result, library=ChartLibrary.PLOTLY)


def update_soc_plot(result) -> go.Figure:
    """更新SOC曲线图（Gradio回调）"""
    if result is None:
        return go.Figure()
    return plot_soc_curve(result, library=ChartLibrary.PLOTLY)


def update_economic_stats(result) -> List[List[str]]:
    """更新经济统计表格（Gradio回调）"""
    if result is None:
        return []
    return generate_statistics_table(result, "economic")


def update_operation_stats(result) -> List[List[str]]:
    """更新运行统计表格（Gradio回调）"""
    if result is None:
        return []
    return generate_statistics_table(result, "operation")
```

---

## 6. 文件结构

```
utils/
├── __init__.py
├── visualization.py         # 本模块主文件
├── chart_styles.py          # 图表样式配置
├── export.py                # 数据导出功能
└── _ext_utils/
    └── __init__.py          # 🔲 扩展工具

templates/                   # 🔲 报告模板（预留）
├── report_default.html
└── report_detailed.html
```

---

## 7. 扩展预留

### 7.1 综合仪表板（预留）

```python
# 🔲 综合仪表板（预留）
def plot_combined_dashboard(result, style: Optional[ChartStyle] = None):
    """
    绘制综合仪表板

    包含：
    - 功率曲线
    - SOC曲线
    - 能量统计饼图
    - 经济指标卡片
    - 关键数据表格
    """
    raise NotImplementedError("综合仪表板待实现")
```

### 7.2 报告生成（预留）

```python
# 🔲 PDF报告生成（预留）
def generate_report(
    result,
    template: str = "default",
    output_path: str = "report.pdf"
) -> str:
    """
    生成PDF报告

    Args:
        result: 仿真结果
        template: 报告模板
        output_path: 输出路径

    Returns:
        str: 生成的报告路径
    """
    raise NotImplementedError("报告生成功能待实现")
```

### 7.3 交互式图表增强（预留）

```python
# 🔲 交互式图表增强（预留）
def create_interactive_dashboard(result) -> Any:
    """
    创建交互式仪表板

    功能：
    - 时间范围选择
    - 数据缩放
    - 数据点详情弹窗
    - 图表联动
    """
    raise NotImplementedError("交互式仪表板待实现")
```

---

## 修改记录

| 版本 | 日期 | 修改内容 | 修改人 |
|------|------|----------|--------|
| v0.1 | 2026-01-08 | 初稿 | Claude |
| v0.2 | 2026-01-08 | 增加教学字段、图表教学标注、交互提示说明、知识点关联 | Claude |
